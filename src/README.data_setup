Run the data_setup.py script as
$ python data_setup.py --afile <name of annotation file> [--path <optional path to GunBlockModel dir>]

Next, tar up the gundata/ directory and move it into the GunBlock model section 
of the Mask_RCNN code.
